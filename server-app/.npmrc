; pnpm node linker: hoisted = flat node_modules like npm, no symlinks.
; Required so the bundled node_modules can be copied into the Electron
; installer package (installer/resources/app/server-app/node_modules)
; without broken symlinks on Windows.
node-linker=hoisted
