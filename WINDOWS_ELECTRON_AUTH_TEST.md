# اختبار المصادقة على Windows — Electron Production Build
# OneSoft ERP — Task #207

**يُنفَّذ هذا الملف يدوياً على جهاز Windows بعد بناء ملف EXE.**  
**تاريخ الاختبار:** ___________  
**رقم الإصدار / اسم الملف:** ___________  
**اسم المختبِر:** ___________

---

## تعليمات التشغيل

1. شغّل ملف `.exe` من مجلد الإصدار
2. انتظر ظهور Splash Screen ثم شاشة الدخول
3. نفّذ الاختبارات بالترتيب وسجّل النتيجة في عمود "النتيجة الفعلية"
4. للتحقق من Launch ID: افتح Console الـ Electron (في DEV فقط) وابحث عن `[auth] Electron Launch ID:`

---

## جدول الاختبارات

| # | الاختبار | الخطوات التفصيلية | النتيجة المتوقعة | النتيجة الفعلية | ناجح/فاشل |
|---|---|---|---|---|---|
| 1 | تشغيل EXE وظهور شاشة الدخول | شغّل `.exe` → انتظر اكتمال التشغيل | شاشة الدخول تظهر بدون خطأ | | |
| 2 | Launch ID أول تشغيل | افتح Developer Console → ابحث عن `[auth] Electron Launch ID:` | يظهر UUID مثل: `a1b2c3d4-...` — سجّل القيمة هنا: __________ | | |
| 3 | تسجيل الدخول | أدخل اسم المستخدم + كلمة المرور → تسجيل الدخول | الانتقال لصفحة البداية | | |
| 4 | تسجيل الخروج | انقر زر "تسجيل الخروج" | شاشة الدخول تظهر فوراً | | |
| 5 | Back بعد الخروج | بعد الخروج، اضغط Back في المتصفح المدمج | يبقى في شاشة الدخول (لا يعود للوحة التحكم) | | |
| 6 | Refresh بعد الخروج | بعد الخروج، اضغط F5 أو Ctrl+R | يبقى في شاشة الدخول | | |
| 7 | Reload داخل جلسة نشطة | بعد الدخول، اضغط F5 | يبقى في لوحة التحكم بدون طلب دخول مجدداً | | |
| 8 | إغلاق × وإعادة الفتح | دخول ناجح → إغلاق النافذة بعلامة × → افتح EXE مجدداً | شاشة الدخول (لا auto-login) | | |
| 9 | Launch ID تغيّر بعد إعادة الفتح | في Console بعد TC-8 → ابحث عن `[auth] Electron Launch ID:` | UUID مختلف عن TC-2 — سجّل القيمة: __________ | | |
| 10 | Task Manager Kill وإعادة الفتح | دخول ناجح → Ctrl+Shift+Esc → End Task → افتح EXE | شاشة الدخول (لا auto-login) | | |
| 11 | Launch ID تغيّر بعد Task Manager Kill | في Console بعد TC-10 | UUID مختلف عن TC-2 وTC-9 — سجّل: __________ | | |
| 12 | ADMIN كلمة مرور غير مُعيَّنة | أنشئ مستخدم admin جديد بدون كلمة مرور → ادخل بكلمة فارغة | الدخول ينجح | | |
| 13 | ADMIN بعد تعيين كلمة مرور | عيّن كلمة مرور للمستخدم → حاول الدخول بكلمة فارغة | يُرفض (401) | | |
| 14 | ADMIN بكلمة المرور الجديدة | ادخل بكلمة المرور المُعيَّنة | الدخول ينجح | | |
| 15 | عدم تلوث بيانات بين المستخدمين | دخول مستخدم A → خروج → دخول مستخدم B | بيانات مستخدم A لا تظهر لمستخدم B | | |

---

## ملاحظات إضافية

```
تشغيل أول — Launch ID:
________________________________________________

إغلاق × ثم تشغيل — Launch ID:
________________________________________________

Task Manager Kill ثم تشغيل — Launch ID:
________________________________________________

ملاحظات أخرى:
________________________________________________
________________________________________________
```

---

## توقيع المختبِر

**الاسم:** ___________________________  
**التاريخ:** ___________________________  
**النتيجة الإجمالية:** ☐ ناجح كلياً &emsp; ☐ فاشل — انظر الملاحظات
