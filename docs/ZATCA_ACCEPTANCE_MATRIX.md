# مصفوفة اختبارات قبول ZATCA

**الإصدار:** مرحلة نموذج وحدة الربط وخدمة `resolveZatcaContext` — مغلقة داخليًا فقط
**تاريخ المرجع:** 2026-08-01  
**المصدر:** `attached_assets/Pasted--ZATCA--1785559422350_1785559422352.txt`
**التوضيح المعماري:** `attached_assets/Pasted--ZATCA--1785559529529_1785559529530.txt`
**مراجعة الحل:** `attached_assets/Pasted--resolveZatcaContext--1785559883386_1785559883390.txt`

## طريقة قراءة الحالة

| الحالة | المعنى |
|---|---|
| `مُثبت الآن` | يمكن إثباته في هذه المرحلة باختبار أو فحص موجود |
| `مُثبت جزئيًا` | يوجد أساس أو اختبار، لكن دورة الفاتورة الكاملة لم تُنفذ |
| `شرط لاحق إلزامي` | لا يُعتبر متحققًا قبل تنفيذ مرحلته واختباره بالمصدر الرسمي |
| `محجوب` | لا يسمح بالانتقال إلى Production قبل نجاحه |

> **تنبيه نطاق:** إغلاق نموذج الربط لا يعني أن تكامل ZATCA جاهز للإنتاج. OTP وCSR والشهادات والمفاتيح الفعلية وXML وQR والتوقيع وSDK وQueue وReporting وClearance وFatoora Simulation والاتصال الفعلي ما زالت خارج التنفيذ.

## مصفوفة القبول

| # | المتطلب | اختبار القبول | الحالة الحالية | دليل/المرحلة التالية |
|---:|---|---|---|---|
| 1 | اعتماد Electronic Invoice Data Dictionary وXML Implementation Standard وZATCA SDK كمصدر الحقيقة | كل قاعدة XML/QR/CSR تشير إلى إصدار رسمي لا إلى قواعد عامة فقط | شرط لاحق إلزامي ومحجوب | لا توجد طبقة XML في مرحلة الربط الحالية |
| 2 | طبقة `ZATCA compliance rules` قابلة للإصدار والتحديث وتسجيل إصدار المواصفات والـ SDK مع Build | تغيير إصدار القواعد يظهر في Build metadata ويرفض تعارض الإصدار | شرط لاحق إلزامي ومحجوب | تُنفذ مع طبقة XML/SDK |
| 3 | منع تعديل/حذف الفاتورة بعد UUID وICV وXML، وحفظ Tax Snapshot | محاولة التعديل/الحذف ترفض، والتصحيح يمر بإشعار دائن/مدين | شرط لاحق إلزامي | لا توجد دورة إصدار إلكتروني في هذه المرحلة |
| 4 | منع تشغيل مركز ZATCA أو الإصدار مع كلمة مرور فارغة/افتراضية وإدارة جلسات فعلية | مستخدم غير موثق أو كلمة مرور محظورة لا يصل للمركز أو الإصدار | شرط أمني لاحق ومحجوب | يراجع مع مسار المصادقة قبل التفعيل |
| 5 | EGS واحد لكل وحدة يضم دفاتر المبيعات والمردود والدائن والمدين، مع ICV/PIH مشتركين ومتسلسلين | الدفاتر الأربعة تعيد EGS نفسه؛ وإصداران متزامنان لا ينتجان ICV متكررًا | مُثبت جزئيًا | اختبار `resolveZatcaContext` يثبت الدفاتر الأربعة؛ ICV/PIH يحتاجان مسار الإصدار |
| 6 | مساران منفصلان: Standard Clearance فوري وSimplified Reporting خلال 24 ساعة | Standard لا يسلم النسخة النهائية قبل Clearance؛ Simplified يملك عداد مهلة وتنبيه تجاوز | شرط لاحق إلزامي ومحجوب | يُنفذ مع Queue والإرسال |
| 7 | SHA-256 وECDSA-256 وXAdES B-B Enveloped وتوقيع XML باستثناء QR، مع سلسلة شهادة وإلغاء | XML موقّع يطابق الخوارزميات، وQR لا يدخل في التوقيع، وشهادة ملغاة ترفض | شرط لاحق إلزامي ومحجوب | يُثبت عبر ZATCA SDK وSimulation |
| 8 | المفتاح الخاص Non-exportable وتجديد CSID بمفتاح وCSR جديدين | لا يوجد endpoint يعيد المفتاح؛ التجديد ينتج key/CSR جديدين ولا يعيد القديم | شرط لاحق إلزامي ومحجوب | يحتاج مخزن مفاتيح آمن ومسار CSID |
| 9 | CRL/OCSP وتسجيل آخر تحديث ومنع الشهادة المنتهية/الملغاة | شهادة منتهية أو ملغاة ترفض، وآخر تحديث CRL/OCSP محفوظ | مُثبت جزئيًا | انتهاء الشهادة مغطى في resolver؛ CRL/OCSP لاحق |
| 10 | OAuth 2 Basic Authentication وفق ZATCA وإخفاء CSID/Secret | لا يظهر Client ID/Secret في logs أو responses، وتفشل بيانات OAuth غير الصالحة | شرط لاحق إلزامي ومحجوب | يُنفذ مع الاتصال الرسمي |
| 11 | تحقق CSR الكامل: EGS serial وVAT وOU وCountry وFunctionality Map وLocation وIndustry | CSR ناقص أو مخالف يرفض قبل طلب التهيئة | شرط لاحق إلزامي | يُنفذ مع CSR/SDK |
| 12 | QR TLV + Base64، Tags 1-9، حد 700 حرف، ومطابقة XML والتوقيع | فك QR يعيد القيم نفسها، والطول لا يتجاوز 700، والقيم تطابق XML | شرط لاحق إلزامي | لا يُستخدم QR الحالي كدليل امتثال |
| 13 | دورة الشهادة: إصدار وتجديد وإلغاء واختراق وإيقاف واستبدال دون فقد ICV/PIH/Queue | كل انتقال مسجل وقابل للاستعادة دون كسر السلسلة | شرط لاحق إلزامي | يُنفذ مع دورة الشهادة والـ Queue |
| 14 | أرشيف ضريبي غير قابل للتعديل لكل XML/QR/طلب/رد/تحذير/خطأ/UUID/ICV/PIH/Hash/Request ID/طباعة | السجل immutable ويحتوي النسخ الأصلية والموقعة والمعادة | شرط لاحق إلزامي | يحتاج جدول أرشيف وسياسة retention |
| 15 | سجل الحوادث والأعطال | يحفظ بداية/نهاية العطل والفواتير المتأثرة وما أُرسل بعد العودة وتقرير الإبلاغ | شرط لاحق إلزامي | يُنفذ مع Queue وIncident log |
| 16 | مصفوفة الحالات Standard/Simplified/Credit/Debit/Prepayment/Export/Nominal/Summary/Self-billing/Third-party/Exemption/Zero Rate/Foreign Currency | كل حالة مدعومة لها XML اختبار؛ غير المدعومة ترفض برسالة واضحة | شرط لاحق إلزامي ومحجوب | يُنفذ قبل أي إصدار حقيقي |
| 17 | التسلسل Unit Tests → ZATCA SDK → Official Fatoora Simulation → Production | لا يسمح Gate الانتقال التالي قبل نجاح السابق | محجوب | Unit Tests الحالية ناجحة؛ SDK وFatoora وProduction لم تُنفذ |

## مصدر الحقيقة ومكان إدارة الربط

| المساحة | المسموح | الممنوع |
|---|---|---|
| مركز الفوترة الإلكترونية — ZATCA → نقاط البيع ووحدات الربط | إنشاء وحدة الربط، اختيار المخزن، تجميع الدفاتر الأربعة، ربط EGS/CSID، ثم OTP والشهادة والمفاتيح في مراحلها | — |
| دفاتر المستندات | عرض حالة الربط واسم الوحدة والمخزن/الفرع ودور الدفتر والبيئة وحالة EGS والشهادة، مع اختصار `/cfg/zatca-center` | اختيار CSID أو الشهادة أو المفتاح، OTP، إنشاء EGS، الإعدادات الفنية، أو تكرار مركز ZATCA |

الـ API الحالي للعرض هو:

```text
documentJournals.getZatcaLinkStatus({ journalId })
```

وهو قراءة فقط ولا يعيد CSID أو الشهادة أو المفاتيح أو الأسرار. أما عمليات الإنشاء والتعديل والربط والفك فموجودة في Router مركز ZATCA فقط.

المخزن هو الفرع ومصدر الحقيقة الوحيد في مسار التوجيه؛ يعاد من API كـ`warehouseId` و`warehouseName` ويعرض في الواجهة بعنوان «المخزن/الفرع». لا يعتمد التوجيه الجديد على `branchId` ولا يقدمه كاختيار مستقل.

## قيود قاعدة البيانات في 0060 و0061

`0060_zatca_pos_linking_units.sql`:

- ينشئ `zatca_pos_units` مع `org_id`, `warehouse_id`, `unit_code`, `unit_name`, الحالة وحقول التدقيق.
- يضيف `document_journals.zatca_pos_unit_id`.
- يضيف `zatca_devices.pos_unit_id`.
- يضيف فهارس الربط والفهارس الفريدة للوحدة/الكود، دفتر/وحدة/نوع، وEGS نشط/وحدة.
- كل العلاقات Foreign Keys غير مدمرة، مع `ON DELETE SET NULL` أو `RESTRICT` أو `CASCADE` حسب الملكية.
- لا يحتوي `DROP` أو `DELETE`، ولا يحول البيانات القديمة؛ يحتفظ بالحقول والجداول القديمة ومنها `zatca_devices.user_id`.

`0061_zatca_pos_linking_integrity.sql`:

- يضيف 8 فهارس ownership مركبة على المنظمات والمخازن والدفاتر ووحدات الربط وEGS والبيئة وCSID والشهادات.
- يثبت أن الوحدة والدفتر ينتميان إلى نفس المنظمة والمخزن.
- يثبت أن `document_journals.zatca_pos_unit_id` لا يرتبط بوحدة من منظمة أخرى.
- يثبت أن مخزن الدفتر يساوي مخزن وحدة الربط، وأن الربط يتطلب `warehouse_id`.
- يقصر الدفاتر المرتبطة على أدوار المبيعات والمردود والدائن والمدين.
- يثبت أن EGS والبيئة وCSID والشهادة تنتمي إلى المنظمة نفسها.
- يشترط وجود بيئة عند ربط EGS بوحدة.
- يعتمد الفهرس الفريد الجزئي لمنع أكثر من EGS فعال لنفس الوحدة.
- يستخدم قيودًا `NOT VALID` عند الإضافة حتى لا تفشل القاعدة بسبب بيانات قديمة؛ هذا يعني أن PostgreSQL يعرض `convalidated=false` عمدًا، مع استمرار فرض القيد على كل `INSERT/UPDATE` جديد. تم فحص وجود القيود والفهارس مباشرة في قاعدة التطوير، واختبار مساري fresh/upgrade.

### شرط قبول البيانات القديمة قبل الإنتاج

`convalidated=false` ليس دليل سلامة للبيانات القديمة. قبل أي اعتماد إنتاجي يجب:

1. إصدار تقرير بكل قيود `0061` التي ما زالت `convalidated=false`.
2. فحص البيانات القديمة مقابل كل قيد وتقرير أرقام ومعرفات المخالفات.
3. اعتماد إصلاح محافظ لكل مخالفة دون حذف عشوائي.
4. إعادة الفحص بعد الإصلاح.
5. تشغيل `VALIDATE CONSTRAINT` فقط بعد خلو التقرير واعتماد الإصلاح.
6. إثبات أن جميع القيود المطلوبة أصبحت `convalidated=true`.

الأداة `pnpm audit:zatca-0061` قراءة فقط، ولا تنفذ `VALIDATE CONSTRAINT` ولا تعدل البيانات. لا يجوز تشغيل التحقق قبل مراجعة البيانات القديمة المجهولة.

لا توجد علاقة تشغيلية مستقلة بين `warehouseId` و`branchId` في مسار التوجيه الجديد.

## اختبارات مرحلة نموذج الربط الحالية

الاختبارات الآلية الحالية في:

```text
server-app/src/tests/zatca-context.test.ts
```

وتغطي:

1. وحدة واحدة ومخزن واحد وEGS واحد.
2. مخزن واحد ووحدتان `EGS-01` و`EGS-02`.
3. استخدام EGS نفسه لدفاتر:
   - `sales_invoice`
   - `sales_return`
   - `credit_note`
   - `debit_note`
4. صلاحية المسؤول.
5. رفض المستخدم غير المصرح.
6. رفض الدفتر غير المرتبط.
7. رفض أكثر من ربط فعال.
8. رفض وحدة بلا EGS.
9. رفض اختلاف البيئة.
10. رفض Production عند غياب Production CSID.
11. رفض الشهادة المنتهية.
12. رفض اختلاف المؤسسة.
13. رفض اختلاف المخزن.
14. رفض الشهادة غير الفعالة، مع إثبات عدم تسريب `privateKey` أو `secretKey`.

وتوجد مصفوفة قبول مسماة إضافية في:

```text
server-app/src/tests/zatca-acceptance-matrix.test.ts
```

نتيجتها الحالية `13/13`، وتشمل:

1. دفتر واحد في مخزن واحد → `EGS-01`.
2. مجموعتا دفاتر مختلفتان في المخزن نفسه → `EGS-01` و`EGS-02`.
3. مدير بصلاحية الدفترين.
4. توجيه `journalId` من مجموعتي دفاتر مختلفتين.
5. أدوار المبيعات والمردود والدائن والمدين لمجموعة واحدة.
6. اختلاف EGS بين مجموعتي دفاتر.
7. عدم التصريح مع سبب `JOURNAL_NOT_ALLOWED` الذي يتحول إلى `FORBIDDEN` في API.
8. دفتر غير مرتبط.
9. أكثر من ربط فعال.
10. عزل مؤسستين.
11. فحص عدم وجود mutation عام داخل Router الدفاتر.
12. إثبات بقاء إدارة الربط في Router مركز ZATCA.
13. رفض مجموعة دفاتر تحتوي دفترًا من مخزن مختلف.

النتيجة الكلية لاختبارات الخادم بعد إضافة حواجز البيئة واتساق المجموعة: `73/73`.

## أمثلة عقد API

### `documentJournals.getZatcaLinkStatus`

استجابة دفتر مرتبط:

```json
{
  "journalId": 100,
  "linkStatus": "linked",
  "posUnit": { "id": 1, "code": "EGS-01", "name": "وحدة ربط 1" },
  "warehouse": { "id": 7, "name": "المخزن الرئيسي" },
  "environment": "محاكاة",
  "egs": { "id": 20, "name": "EGS-01", "status": "active" },
  "certificate": { "id": 50, "status": "active" },
  "openCenterPath": "/cfg/zatca-center"
}
```

استجابة دفتر غير مرتبط:

```json
{
  "journalId": 101,
  "linkStatus": "unlinked",
  "posUnit": null,
  "warehouse": { "id": 7, "name": "المخزن الرئيسي" },
  "environment": null,
  "egs": null,
  "certificate": { "id": null, "status": "غير مرتبطة" },
  "openCenterPath": "/cfg/zatca-center"
}
```

لا تحتوي الاستجابتان على `CSID`, `Secret`, `Private Key`, `OTP`, أو `Certificate Content`.

### `resolveZatcaContext`

```text
journalId=100
→ zatcaPosUnitId=1
→ warehouseId=7 (اتساق الدفتر مع الوحدة)
→ egsDeviceId=20
→ environmentId=30 / Simulation
→ activeCsidId=40
→ certificateId=50
```

تعيد النتيجة معرفات وحالة الشهادة فقط، ولا تعيد قيمة CSID أو secret أو private key أو محتوى الشهادة.

## اختبارات الربط الخلفية

| السيناريو | المتوقع |
|---|---|
| إنشاء وحدة بمخزن من مؤسسة أخرى | رفض |
| ربط دفتر بنوع غير مخصص لـ ZATCA | رفض |
| ربط دفتر بلا مخزن | رفض |
| ربط دفتر من مخزن مختلف | رفض |
| ربط دفتر مرتبط بوحدة أخرى | رفض |
| وجود EGS فعالين لنفس الوحدة | رفض عند الحل |
| فك الربط | إزالة الربط فقط، دون حذف الدفتر أو بيانات ZATCA القديمة |
| طلب إنشاء/تعديل دفتر مع `zatcaPosUnitId` من Router الدفاتر | لا يكون حقل إدارة مقبولًا؛ إدارة الربط تتم من مركز ZATCA فقط |
| قراءة `getZatcaLinkStatus` | يعيد linked/unlinked، الوحدة، المخزن، الدور، البيئة، EGS، الشهادة، ورابط المركز دون أسرار |

واجهة `documentJournals` لا تقبل `zatcaPosUnitId` في `create/update`، ولا توجد bulk mutation أو admin endpoint أخرى لتعديل هذا الحقل. عمليات الإنشاء والتعديل والربط والفك موجودة في Router مركز ZATCA فقط.

لا توجد شاشة ربط كاملة داخل دفتر المستندات في هذه المرحلة، وتبويب «الفوترة الإلكترونية» مؤجل لمرحلة الواجهات ولا يعتبر مكتملًا الآن.

## بوابة Production

حالة البوابة الحالية: **مغلقة**.

لا يكفي نجاح Mock Sandbox أو اختبارات الوحدة لاعتماد الامتثال. يجب تنفيذ وتسليم الأدلة التالية بالترتيب:

```text
1. Unit Tests
2. ZATCA SDK validation
3. Official Fatoora Simulation
4. مراجعة كل حالات الفواتير المدعومة
5. موافقة صريحة قبل Production
```

حتى اكتمال هذه البنود، تبقى عمليات OTP والإرسال الحقيقي وProduction خارج التنفيذ.

## حالة النشر وحماية البيانات

النشر الحالي في Replit تم التحقق من ظهوره كـ`public`. لذلك يمنع هذا المشروع في هذه المرحلة تخزين أو اختبار OTP أو CSID أو المفاتيح أو الشهادات أو XML الحقيقي أو بيانات العملاء الحقيقية على نسخة النشر العامة. المرحلة التالية تستخدم OTP محاكى فقط، ولا يبدأ الربط الفعلي قبل تأمين بيئة النشر واعتماد طريقة حفظ Secrets.

## المنجز المقبول مقابل غير المنجز

**المنجز المقبول:**

- نموذج وحدة ربط الدفاتر.
- توجيه الربط من `journalId`.
- التحقق من الصلاحية والمخزن والبيئة.
- اختيار EGS ومرجع CSID داخليًا.

**غير المنجز:**

- OTP وCSR.
- الشهادات والمفاتيح الفعلية.
- XML وQR والتوقيع.
- SDK وQueue.
- Reporting وClearance وحالات رد الهيئة.
- Fatoora Simulation الرسمية وProduction.
